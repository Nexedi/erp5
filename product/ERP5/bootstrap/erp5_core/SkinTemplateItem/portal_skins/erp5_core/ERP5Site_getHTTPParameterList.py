# Disabled until a correct API for html forms is defined.
#for k, v in request_form.items():
#  kw[k]=v
#if kw.has_key('-C'):
#  del kw['-C'] # Why is something like that in the request ? What does it mean ?
return kw
