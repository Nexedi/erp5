return context.Resource_addToShoppingCart(resource=resource,
                                          quantity=quantity,
                                          form_id=form_id,
                                          checkout=True)
